import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Radar IQ | InvestIQ', description: 'Feed de noticias, alertas CVM e insights do motor.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
