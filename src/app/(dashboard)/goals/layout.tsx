import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Metas | InvestIQ', description: 'Planeje metas financeiras e simule cenarios FIRE.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
