import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Carteiras Inteligentes | InvestIQ', description: 'Portfolios pre-construidos baseados em regime macro e IQ-Score.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
