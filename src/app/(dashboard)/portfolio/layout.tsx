import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Carteira | Invística', description: 'Gerencie posicoes, acompanhe performance e diagnosticos.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
