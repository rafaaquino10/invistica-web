import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Mapa de Mercado | Invística', description: 'Heatmap interativo do mercado B3 por setor e capitalizacao.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
