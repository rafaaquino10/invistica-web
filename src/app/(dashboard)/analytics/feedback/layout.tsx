import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Feedback Loop | InvestIQ', description: 'Acuracia e evolucao do modelo de scoring.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
