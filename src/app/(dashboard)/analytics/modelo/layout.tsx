import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Transparencia do Modelo | InvestIQ', description: 'IC Spearman, hit rate, signal decay e walk-forward do IQ-Cognit.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
