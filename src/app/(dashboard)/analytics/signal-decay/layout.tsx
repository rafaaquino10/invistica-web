import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Signal Decay | InvestIQ', description: 'Monitoramento do poder preditivo por pilar.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
