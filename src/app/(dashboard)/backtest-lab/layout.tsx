import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Backtest Nuclear | InvestIQ', description: 'Simulacao historica com scoring point-in-time e stress test.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
