import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Comparar Ativos | InvestIQ', description: 'Compare ate 4 acoes lado a lado com scores e fundamentals.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
