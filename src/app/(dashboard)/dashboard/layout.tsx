import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Dashboard | InvestIQ', description: 'Visao geral da carteira, performance e oportunidades.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
