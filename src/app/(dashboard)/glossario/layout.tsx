import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Glossario | InvestIQ', description: 'Termos e conceitos do mercado financeiro brasileiro.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
