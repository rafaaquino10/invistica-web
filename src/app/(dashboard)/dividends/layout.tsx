import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Dividendos | Invística', description: 'Calendario, projecoes e simulador de renda passiva.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
