import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Configuracoes | Invística', description: 'Perfil, preferencias e notificacoes.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
